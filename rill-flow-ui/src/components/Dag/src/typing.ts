export enum MODE {
  DEFINITION = "definition",
  INSTANCE = "instance"
}

export enum RILL_CATEGORY {
  FUNCTION = "function",
  FOREACH = "foreach",
  RETURN = "return",
  PASS = "pass",
  SUSPENSE = "suspense",
  CHOICE = "choice",
}
